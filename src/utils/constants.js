export const shortDescription = `${"React-Component-Graphify"} works by scanning your entire src/ directory for React component files, identifying import relationships, and generating a clear visual tree showing which components import which — and where.

It maps your component architecture with line-level accuracy, watches for changes in real time, and writes the structure to a neatly formatted output file.

It’s zero-runtime, non-intrusive, and built for clarity and maintainability — no configuration required.`;
